# @package _global_

# to execute this experiment run:
# python train.py experiment=example

# Tessera with average encoder and linear prediction head, for tile sizes 128 and 256, each with 3 random seeds

defaults:
  - override /model: s2bms_prediction
  - override /data: s2bms_prediction
  - override /metrics: s2bms_predictive

tags:
  - prediction
  - tessera
  - tessera_${str:${data.dataset.modalities.tessera.size}}
  - ${str:${seed}}

experiment_name: avr_tessera_${str:${data.dataset.modalities.tessera.size}}_linear

model:
  geo_encoder:
    _target_: src.models.components.geo_encoders.average_encoder.AverageEncoder
    geo_data_name: tessera
  trainable_modules: [prediction_head]
  prediction_head:
    _target_: src.models.components.pred_heads.linear_pred_head.LinearPredictionHead
    hidden_dim: 512

data:
  dataset:
    modalities:
      tessera:
        format: npy
        enable_nans: true
    use_target_data: true
    use_aux_data: false
  caption_builder:
  pin_memory: false
  num_workers: 14
  batch_size: 128
  persistent_workers: false
  saved_split_file_name: "s2bms_unlabelled_union_val_test.pth"

logger:
  wandb:
    tags: ${tags}
    group: "predictive"
    project: "s2bms_prediction"
  aim:
    experiment: "predictive"

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      seed: 12345, 404, 654
      +data.dataset.modalities.tessera.size: 128, 256
