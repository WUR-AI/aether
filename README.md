# aether

Based on vdplasthijs/PECl repo.

The python environment is saved in `aether.yml`. 

To use Google Earth engine utilities, you need to create a file `content/api_keys.py` that contains one line `GEE_API = '<your_api_key>'`. 